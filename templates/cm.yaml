apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}
data:
  test: test123
  name: "{{ .Files.Get "files/new.txt" }}"
  index: "{{ index .Files "files/new.txt" | toString | repeat 5 }}"
  bytes: "{{ .Files.GetBytes "files/new.txt" | b64enc }}"
